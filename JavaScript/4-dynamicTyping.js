// Programming languages that aloow you to initialize a variable to a data type and then reassign it to a different data type are said to be DYNAMICALLY TYPED
// the type of variable can change at runrime since it is dynamically typed
let firstName = "Victor";
console.log(typeof firstname);
firstName = 18;
console.log(typeof firstname);
firstName = true; 
console.log(typeof firstname);
// i useed the typeof keyword so we can see the data type at each point in the code
